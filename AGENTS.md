# 노동사건자동화

법무법인 평안 노동팀 업무 자동화 시스템. GPT(Codex)와 Claude Code가 같은 업무 규칙·사건 자료·실행 코드를 사용한다. 이 파일이 루트 공통 규칙의 원본이고, 루트 `CLAUDE.md`는 자동 동기화본이다. 폴더별 업무 규칙의 원본은 기존 `{폴더}/CLAUDE.md`이며, `{폴더}/AGENTS.md`가 그 파일로 안내한다.

## 폴더

| 폴더 | 하는 일 | 산출물 | 하이라이트 색 |
|---|---|---|---|
| `1_검토의견` | 의뢰인 질의에 대한 검토의견 작성 | 이메일 회신 초안 / 검토의견서(.docx) | 노랑 |
| `2_판례검색` | 질의 관련 판례를 찾아 **나열만** | 채팅 답변 하나 | 파랑 |
| `3_서면보강` | 사용자가 쓴 서면의 주장을 보강할 판례 검색 | 채팅 답변 하나 | 보라 |
| `4_서면작성` | 준비서면·이유서·답변서 작성 (라운드별 공방) | 서면 본문 .docx | 연두 |
| `5_녹취록` | 녹음파일 → 전사 → 화자 배정 → 녹취서 | 녹취서 .md (간이는 녹취록 .md) | — |
| `6_손해배상계산` | 대법원 계산프로그램 재현, 손해배상액·노동 금액(수당·퇴직금·연차수당 차액, 해고기간 임금, 지연손해금) 계산·검산 | 계산표 .xlsx | — |

## 설치

설치·이전 절차는 `시작하기.md`, 구조와 동기화 규칙은 `README.md` 에 있다.

## 작업 시작 방법

루트에서 `/서면 ...`처럼 요청한다. `공통/명령어/{명령어}.md`를 찾아 읽고 해당 업무 지침을 적용한다. Claude에서는 생성된 슬래시 명령어가 연결되고, Codex에서는 `labor-workflow` 스킬 또는 이 문장의 경로 안내가 연결한다. 슬래시 메뉴 등록과 텍스트 요청의 해석은 구분한다. `$ARGUMENTS`·`$1`은 사용자가 전달한 인자를 뜻하며 그대로 셸에 삽입하지 않는다.

| 명령어 | 폴더 |
|---|---|
| `/검토의견` | 1_검토의견 |
| `/판례검색` | 2_판례검색 |
| `/서면보강` | 3_서면보강 |
| `/서면` `/증거정리` `/증거발췌` | 4_서면작성 |
| `/녹취` | 5_녹취록 |
| `/손배계산` `/손배검산` | 6_손해배상계산 |
| `/문체검증` | 4_서면작성 |
| `/결과비교` | 공통/결과비교.md |

시스템 운영은 `공통/운영.md`를 따른다. 사건 작업을 시작할 때는 `공통/사건상태.md`를 읽어 최초 분석인지 변경분 처리인지 구분한다. 비교 요청에만 `공통/결과비교.md`를 추가로 읽는다. 업무와 무관한 지침을 한꺼번에 읽지 않는다.

## 공통 규칙

**금액을 직접 계산하지 않는다.** 어느 폴더에서 다루든 아래 금액은 `6_손해배상계산/engine/`이 계산한다. 끝수처리·일할 방식·기산일이 항목마다 달라 암산이나 어림으로는 반드시 틀린다. 검토의견이나 서면에 금액이 필요하면 `/손배계산`을 거쳐 그 출력을 옮긴다.

- 신체손해 배상: 일실수입·호프만 계수·과실상계·공제
- 노동 금액: 통상시급, 연장·야간·휴일수당 차액, 평균임금·법정 퇴직금 차액, 미사용 연차휴가수당, 해고기간 임금 상당액과 중간수입 공제, 지연손해금(연 20%·6%·5%·소송촉진법 이율)과 소멸시효 만료일

엔진이 다루지 않는 금액(해고예고수당, 휴업수당 자체, 최저임금 차액 등)은 파이썬으로 계산하되 산식·끝수·기산일·근거 자료(파일·쪽)를 표로 함께 남기고 '엔진 미지원 — 사람 검산 필요'로 표시한다. 본문에 옮기는 숫자는 그 표의 값만 쓴다.

엔진이 판례가 갈리는 지점에서 옵션으로 고른 금액(계산표 '근거' 시트의 불명확 표시)은 결론을 단정하지 않는다. 검토의견은 조건절('~로 본다면 …원으로 보입니다')로, 서면은 하단 `[취약 논리 및 보완 방향]`에 대안 계산액과 반대 판결을 적는다.

**LBOX 검색은 `lbox-검색` 스킬을 따른다.** 네 폴더가 같은 규칙을 쓰므로 폴더별로 복사해 두지 않는다. 하이라이트는 `lbox-하이라이트` 스킬을 따른다.

**LBOX 내 AI 기능(질의·문서 생성)은 어느 폴더에서도 쓰지 않는다.** 통합 입력창의 '검색' 모드만 쓴다.

**하이라이트 색은 작업 유형을 뜻하며 폴더마다 고정이다.** 칠하려는 부분에 이미 하이라이트가 있으면 **색에 관계없이 기존 것을 지우고 이번 작업 색으로 새로 칠한다.** 하이라이트는 사용자가 이번 작업이 실제로 수행되었는지 직접 검증하는 절차이므로, 남아 있던 하이라이트를 그대로 두면 이번에 칠한 것인지 예전 것인지 구별되지 않는다.

**근거를 찾지 못한 쟁점은 결론을 단정하지 않는다.** 근거가 확인되지 않는다는 점을 먼저 밝히고 다툼의 여지가 있다는 취지로 맺는다. 다만 '판례가 확인되지 않는다'고 쓰기 전에 `lbox-검색` 스킬의 단계를 모두 이행하였는지 점검한다. 검색을 덜 한 것을 판례가 없는 것으로 적으면 검색 실패가 검토 결과로 둔갑한다.

**판결·결정문 속 당사자 주장을 법원 판단처럼 인용하지 않는다.** 판례·결정례·재결례·행정해석에서 문장을 가져올 때에는 원문에서 그 문장이 어느 부분에 있는지 먼저 확인한다. '…라고 판단하였다', '…라고 판시하였다', '…한 재결례가 있다'로 쓸 수 있는 것은 법원·위원회가 스스로 판단한 부분(판결요지, 이유 중 판단, 재결요지·판정요지, 회시)뿐이다. 원고·피고·청구인·피청구인의 주장, 처분청의 답변, 인정사실·기초사실에 적힌 경위, 상급심에서 파기·취소된 원심 판단, 판결이 소개한 다른 사건의 설시, 행정해석 질의의 갑설·을설은 그 성격을 드러내어 쓰고(예: '근로복지공단이 …한 사례가 재결에 나타나 있다', '인정사실에 … 경위가 나타나 있다', '원고는 …라고 주장하였으나 받아들여지지 않았다'), 결론을 뒷받침하는 법리로 쓰지 않는다. 스니펫이나 판례기록의 발췌만 보고 판단이라고 단정하지 않는다. 2026. 9. 17. KEPS 검토의견서에서 행정심판 재결의 '피청구인 주장' 부분(근로복지공단이 가산금·연체금을 부과하지 않은 경위)을 재결의 판단처럼 적었다가 인용 검증에서 고쳤다.

**승산·전망 평가에서 낙관적 의견을 함부로 내지 않는다.** '다툴 실익이 충분하다', '인정될 가능성이 높다', '유력하다'를 쓰지 않는다. 유리한 사정과 불리한 사정을 같은 비중으로 제시하고, 사용자 재량이 넓게 인정되는 영역인지·증명책임이 누구에게 있는지·근거가 문서인지 전언인지를 밝힌 뒤, 결과를 예단하지 않는 형태로 맺는다. 의뢰인이 낙관적 답을 기대하더라도 같다.

**문장 종결은 단정형을 피한다.** '~로 사료됩니다', '~로 판단됩니다', '~로 보입니다', '~이 적절해 보입니다'와 같이 완충하여 맺는다. 다만 법령 조문·판례 판시·첨부자료 문언을 그대로 옮기는 문장은 단정형으로 쓴다. 검토자의 법적 부담을 줄이기 위한 것이므로 미사여구 금지와는 별개의 규칙이다.

서면 스킬의 단정형 우선 지시와 충돌하면 위 완충 종결 원칙을 우선한다. 이를 변경하려면 사용자가 문체 범위를 별도로 지정해야 한다. 인용 원문은 바꾸지 않는다.

**미사여구를 쓰지 않는다.** 인사말, 정서적 마무리, 사과·감사 표현을 넣지 않는다.

## 파일 배치 규약

이 폴더는 회사 OneDrive 안에 있고, 회사·집 두 PC가 같은 폴더를 본다. **사건 자료는 git에 올리지 않는다.** 시스템 파일만 git으로 관리하며, 저장소(`.git`)는 OneDrive 밖에 둔다(`공통/git-분리-검토.md`).

- **절대경로를 쓰지 않는다.** OneDrive 경로는 PC마다 다르다(`C:\Users\{사용자}\OneDrive - {회사명}\...`). 지침·명령어·스크립트에는 저장소 루트 기준 상대경로만 쓴다. 셸에서 부득이하면 `$env:OneDrive`를 쓰고, 폴더 이름에 공백과 하이픈이 있으므로 따옴표로 감싼다.
- 사건 자료는 각 폴더의 `사건/` 아래에 둔다. OneDrive가 동기화하므로 별도 조치가 필요 없다.
- **파이썬 가상환경과 음성 모델은 `%LOCALAPPDATA%\노동사건자동화\` 에 두고 이 폴더 안에 만들지 않는다.** 용량이 수 GB이고 PC마다 따로 설치해야 하므로 동기화 대상이 아니다. 다만 화자 분리 모델(35MB)은 예외로 `5_녹취록/models/` 에 둔다(`5_녹취록/setup.ps1`). 용량이 작아 OneDrive 로 옮기는 편이 PC마다 다시 받는 것보다 낫고, git 에는 올리지 않는다.
- 사용자가 시스템 파일을 고치겠다고 하면, 파일 전체를 새로 쓰지 말고 현재 내용을 먼저 확인한 뒤 해당 부분만 고친다.
