# 시작하기 — 무엇을 어떤 순서로 할지

> 운영 규칙은 `공통/운영.md`를 먼저 읽는다. 아래 내용은 설치·이전 절차이다. 새 PC에서는 `python 공통/scripts/system.py check`로 명령어·스킬이 제자리에 있는지 확인한다.

이 zip은 **기존 `C:\노동사건자동화`를 대체하는 것이 아니라 거기에 합쳐 넣는 새 묶음**이다.
아래 순서대로 한다. **1~2단계까지 하면 지침과 명령어는 돌아간다.** 3~6단계는 이 PC에 필요한 프로그램을 까는 일이고 나중에 해도 된다.

---

## 1단계 · zip 풀기

zip을 바탕화면에 풀면 `노동사건자동화` 폴더가 나온다. 아직 아무것도 옮기지 않는다.

---

## 2단계 · OneDrive에 자리 만들기

탐색기에서 OneDrive 폴더를 연다. 회사 계정이면 이런 이름이다.

```
C:\Users\{사용자}\OneDrive - 법무법인 평안\
```

1단계에서 나온 `노동사건자동화` 폴더를 여기로 **잘라내기 → 붙여넣기** 한다.

붙여넣은 뒤 **폴더 우클릭 → "이 디바이스에 항상 유지"** 를 체크한다. 이걸 하지 않으면 OneDrive가 파일을 자리표시자로 바꿔 두어 파이썬이 파일을 읽지 못한다.

트레이의 OneDrive 아이콘이 초록 체크로 바뀔 때까지 기다린다.

`1_검토의견\templates\샘플\` 에 기존 검토의견서 완성본을 두어 건 넣어 둔다. 검토의견서(.docx)로 전환할 때 작업 베이스로 쓴다(`1_검토의견/templates/README.md`). 의뢰인 내용이 들어 있어 git 과 백업 zip 에서는 빠지고, OneDrive 로 두 PC에 넘어간다.

---

## 3단계 · 파이썬 라이브러리 설치

두 폴더가 시스템 파이썬을 함께 쓴다. **둘 다 깐다.** `4_서면작성` 쪽을 빠뜨리면 `/증거정리`·`/증거발췌`가
`ModuleNotFoundError: No module named 'fitz'` 로 바로 멈춘다.

```powershell
cd "$env:OneDrive\노동사건자동화\6_손해배상계산"
pip install -r requirements.txt
python -B -m pytest tests -q -p no:cacheprovider

cd "$env:OneDrive\노동사건자동화\4_서면작성"
pip install -r requirements.txt
```

pytest 는 실패(failed) 없이 모두 통과해야 한다. 서면 쪽은 지금 폴더(`4_서면작성`)에서 아래 한 줄로 확인한다. `usage:` 로 시작하는 사용법이 나오면 된다.

```powershell
python scripts\evidence_check.py --help
```

---

## 4단계 · 노임표·생명표 추출

`6_손해배상계산\노임표-추출-절차.md` 를 보고 한다. **대법원 프로그램이 깔린 PC에서 한 번만** 하면 되고, 결과는 OneDrive로 다른 PC에도 넘어간다.

가장 자주 막히는 곳은 2단계에서 **32비트 PowerShell**로 실행하지 않는 것이다.

```
C:\Windows\SysWOW64\WindowsPowerShell\v1.0\powershell.exe
```

---

## 5단계 · 녹취록 설치

`5_녹취록` 폴더의 **`녹취록 환경설치.bat` 을 더블클릭**한다. PowerShell 실행 정책 때문에 `.ps1` 이 막히는 일을 피한다. 명령줄로 하려면 아래처럼 한다.

```powershell
cd "$env:OneDrive\노동사건자동화\5_녹취록"
powershell -NoProfile -ExecutionPolicy Bypass -File .\setup.ps1
```

전사(faster-whisper)와 화자 분리(sherpa-onnx) 모두 CPU로 돈다. GPU도 HuggingFace 토큰도 필요 없다. 설치가 끝나면 검증 결과의 '화자 분리 가능'이 '예'인지 본다. 설치되는 것과 자주 겪는 문제는 `5_녹취록/설치안내-녹취.md` 에 있다.

---

## 6단계 · LibreOffice 설치 (docx → PDF 확인용)

```powershell
winget install --id TheDocumentFoundation.LibreOffice --exact --accept-package-agreements --accept-source-agreements
```

검토의견서나 서면을 .docx로 만든 뒤 **전 면을 눈으로 확인하려면 PDF로 변환해야 하고, 그 변환을 이것이 한다.** 없으면 `1_검토의견\서면전환-가이드.md`의 `soffice --headless --convert-to pdf` 단계에서 막힌다.

설치 뒤 확인한다.

```powershell
& "$env:ProgramFiles\LibreOffice\program\soffice.exe" --headless --convert-to pdf --outdir . "아무.docx"
```

변환 시 `Could not find platform independent libraries <prefix>` 가 출력되어도 무시한다. 환경변수 `PYTHONHOME` 때문에 LibreOffice 내장 파이썬이 내는 경고일 뿐 변환에는 영향이 없다.

줄바꿈 위치는 Word와 다르다. LibreOffice는 한글을 글자 단위로 끊어 면수가 Word보다 적게 나올 수 있다(2026. 9. 17. 검토의견서 한 건이 LibreOffice 4면, Word 5면). 전 면의 배치를 눈으로 보는 데에는 충분하나, 면수를 맞추거나 최종 출력물의 줄바꿈을 확정해야 하면 Word에서 본다. Word를 자동화로 띄울 때는 `공통/운영.md` '검증 범위'의 Word COM 규칙을 따른다.

---

## 두 번째 PC

같은 OneDrive 계정으로 로그인하면 폴더가 그대로 내려온다. 이 PC에서 할 일은 다섯이다.

1. 폴더 우클릭 → **"이 디바이스에 항상 유지"**
2. **git 저장소 붙이기** — 아래 참조
3. `6_손해배상계산` 과 `4_서면작성` 에서 각각 `pip install -r requirements.txt` (3단계)
4. `5_녹취록` 의 `녹취록 환경설치.bat` 더블클릭 (5단계) — 가상환경은 PC마다 따로 만들고, 전사 모델은 그 PC에서 처음 전사할 때 받는다
5. LibreOffice 설치 (6단계) — PC마다 따로 설치해야 하고 동기화되지 않는다

노임표 CSV·양식·샘플·지침은 이미 동기화되어 있으므로 다시 준비하지 않는다.

### git 저장소 붙이기

시스템 파일은 git으로도 관리한다. `.git` 은 OneDrive 밖에 두므로 **PC마다 한 번씩** 붙여 준다.

```powershell
cd "$env:OneDrive\노동사건자동화"
powershell -ExecutionPolicy Bypass -File "공통\scripts\repo-setup.ps1" -원격 https://github.com/chlfkrqh-ship-it/labor-automation.git -이름 "chlfkrqh" -메일 "chlfkrqh@gmail.com"
```

첫 연결에서 GitHub 로그인 창이 한 번 뜬다. 브라우저에서 승인하면 그 뒤로는 묻지 않는다.

그 뒤로 **git 은 사람이 치지 않는다.** 시스템 파일을 고치면 작업을 맡은 AI 가 올리고, 다른 PC에서 따로 받을 것도 없다
(`공통/운영.md` '시스템 파일 올리기'). 처음 한 번은 Claude 가 `g.bat` 실행 허락을 물으니 '다시 묻지 않기'로 허락한다.

이력을 직접 보고 싶을 때만 `공통\scripts\g.bat log --oneline -10` 을 쓴다.

LBOX는 Claude 데스크톱 앱의 인앱 브라우저를 먼저 쓰고, 로그인되어 있지 않으면 엣지 브라우저로 한다. 한 계정에 동시에 3곳까지만 로그인되어 두 PC의 인앱과 엣지를 모두 로그인해 둘 수 없기 때문이다. 엣지에 Claude in Chrome 확장을 설치하고 lbox.kr 에 로그인해 둔다. 자세한 내용은 `공통/브라우저.md` 에 있다.

---

## 막혔을 때

| 증상 | 볼 곳 |
|---|---|
| `/서면` 이 안 뜬다 | `python 공통\scripts\system.py check` 로 `.claude/commands/` 에 명령어가 있는지 |
| 파이썬이 파일을 못 읽는다 | 2단계 — "이 디바이스에 항상 유지" |
| 노임표 추출 실패 | `6_손해배상계산\노임표-추출-절차.md` 의 실패 표 |
| `soffice` 를 찾을 수 없다 / docx를 PDF로 못 바꾼다 | 6단계 — LibreOffice는 PC마다 설치한다 |
| 전체 구조·규칙 | 루트 `CLAUDE.md`, `README.md` |
