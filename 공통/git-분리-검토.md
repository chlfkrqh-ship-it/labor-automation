# 시스템만 git으로 분리 (시행 기록)

2026. 9. 22. 검토하고 같은 날 시행하였다. 첫 커밋 `023d3b7` 까지 마쳤고, **남은 것은 GitHub 원격 연결 하나**이다(맨 아래).

## 왜

`README.md` 5항이 이미 적어 둔 한계이다. "git을 쓰지 않으므로 Claude Code 웹/모바일로는 이 폴더에 접근할 수 없다." 그래서 집이나 휴대폰에서 이어가려면 회사 PC가 켜져 있어야 한다.

부수적으로 얻는 것이 더 크다.

- `공통/백업/`과 `노동사건자동화-백업\*.zip` 의 수동 백업을 대체한다. 2026. 9. 9. 홈 폴더 purge로 하위까지 지워진 일이 있었다.
- 지침을 고쳤을 때 무엇이 어떻게 바뀌었는지 남는다. 지금은 zip 두 개를 풀어 비교해야 한다.
- `6_손해배상계산`의 pytest 409건을 클라우드에서 돌릴 수 있다. 파이썬만 있으면 되므로 유일하게 클라우드로 통째로 옮겨지는 작업이다.

## 실측

2026. 9. 22. 이 폴더에서 잰 값이다.

| | 값 |
|---|---|
| 전체 | 약 19.3 GB |
| 사건 자료 | 19.2 GB (`4_서면작성/사건` 14.8 GB, `1_검토의견/사건` 4.4 GB) |
| **git 추적 대상** | **471개 파일 · 2.9 MB** |
| `git status` 소요 | 0.09초 |

`.gitignore` 가 사건 폴더를 디렉터리째 제외하므로 git 이 19 GB 안을 들여다보지 않는다. 크기와 속도는 문제가 되지 않는다.

## 먼저 고쳐야 할 것 — `.gitignore` 누락

**지금 상태로 git 을 켜면 의뢰인 자료가 따라 올라간다.** 실측에서 622개가 잡혔고 그중 95개(43 MB)가 사건 자료였다. `.gitignore` 가 사건 폴더 이름을 기준으로만 쓰여 있어, 폴더 이름이 바뀐 곳과 시스템 폴더에 섞여 있는 것을 걸러내지 못한다.

| 걸러지지 않는 것 | 무엇인가 | 왜 빠졌나 |
|---|---|---|
| `2_판례검색/사건/` (94개, 43 MB) | 어느 통상임금 소송의 소장·준비서면·상고이유서 전부 | `.gitignore` 에는 `2_판례검색/결과/` 로 적혀 있다. 폴더 이름이 `사건` 으로 바뀐 뒤 따라가지 못했다 |
| `6_손해배상계산/02_결과/` 1개 | 계산 결과 | `02_결과/*` 와 `!.gitkeep` 사이로 새어 나왔다 |
| `4_서면작성/templates/문체레퍼런스_….pdf` · `.txt` | 의뢰인명이 붙은 실제 서면 | 문체 레퍼런스로 쓰지만 의뢰인 자료이다 |
| `4_서면작성/templates/(의뢰인명) 재심답변서….docx` | 실명이 든 실제 서면 | 같음 |
| `2_판례검색/양식/(리서치) 운전원 휴게시간 관련.docx` | 실제 리서치 결과물 | 같음 |
| `공통/검증/` | 실제 사건으로 시험한 질의·재료 | 검증용이지만 실제 사건 자료이다 |

`1_검토의견/templates/샘플/` 은 "완성본 샘플(의뢰인 내용 포함)" 으로 이미 제외되어 있다. **같은 성격의 파일이 다른 폴더에서는 빠져 있는 것이 문제의 본질이므로, 폴더마다 열거하는 대신 규칙을 하나 더 두는 편이 낫다.**

```gitignore
# 의뢰인 자료는 폴더 이름과 무관하게 제외한다
**/사건/
**/샘플/
**/실제사건*/
문체레퍼런스_*
(*)*          # 파일 이름이 '(의뢰인명)' 으로 시작하면 의뢰인 산출물이다
공통/검증/     # 실제 사건으로 시험한 자료. 의뢰인명이 데이터의 키로 쓰인다(577건)
```

**이름 규칙을 빠져나간 것이 하나 있었다 — `4_서면작성/templates/표준양식.docx`.** 파일 이름에 `(의뢰인명)` 이
없어 `(*)*` 규칙에 걸리지 않은 채 추적되고 있었고, 안에는 실제 사건번호·당사자·변론기일이 들어 있었다.
양식 자체는 서면 생성에 필요하므로 제외하지 않고, **내용을 가공값으로 바꾸어 두었다**
(2026. 9. 22. — 사건번호 `2026구합10001`, 원고 `주식회사 가온`, 피고보조참가인 `홍길동`, 기일 비움).
법인명과 담당변호사는 우리 쪽 정보이므로 그대로 둔다. **앞으로 양식 파일을 추가할 때에는 파일 이름만 보지 말고
문서 안의 당사자 정보를 함께 본다.** 이름 규칙은 파일명만 걸러 내기 때문이다.

**지침과 판례기록에는 의뢰인명이 남아 있다**(239건). 판례기록의 '확인한 사건' 줄과, 지침의 "어느 사건에서 이런 실수가 있었다" 는 교훈 출처이다. 파일이 아니라 사건명 언급이고, 빼면 그 기록의 쓸모가 없어지므로 그대로 두었다.

제외한 뒤 실제 추적 대상이 **471개 2.9 MB** 이다. 큰 것은 `표준양식.docx`(0.1 MB), `wage_occupation.csv`(0.2 MB), 엔진 소스이다.

## 구조 — `.git` 만 OneDrive 밖에 둔다

`README.md` 가 든 이유가 맞다. "OneDrive가 `.git/`을 동기화하면 두 PC의 index·lock 파일이 충돌해 저장소가 깨진다." 그러나 이는 **`.git` 을 OneDrive 안에 두었을 때의 이야기**이고, git 은 저장소와 작업본을 떼어 놓을 수 있다.

```
%LOCALAPPDATA%\노동사건자동화\repo.git\    ← .git (PC마다 따로, 동기화하지 않음)
%OneDrive%\노동사건자동화\                 ← 작업본 (지금 그대로, 아무것도 옮기지 않는다)
```

폴더를 옮기지 않고, 정션도 만들지 않는다. **상대경로 규칙과 기존 스크립트가 전부 그대로 돈다.** 2026. 9. 22. 이 구성으로 `git status` 가 정상 동작하는 것을 확인하였다(0.09초, 471개 인식).

설정은 PC마다 한 번이다.

```powershell
$GD = "$env:LOCALAPPDATA\노동사건자동화\repo.git"
git init --bare $GD
git --git-dir=$GD config core.bare false
git --git-dir=$GD config core.worktree "$env:OneDrive\노동사건자동화"
```

매번 `--git-dir` 을 치지 않도록 래퍼 하나를 둔다(`공통/scripts/g.ps1` 정도). `g status`, `g add -A`, `g commit`, `g push` 로 쓴다.

두 PC는 **git 으로도 동기화되고 OneDrive 로도 동기화된다.** 시스템 파일이 두 경로로 오가면 혼란스러우므로, 아래 둘 중 하나를 정해야 한다.

- **A안 — OneDrive 를 주로 두고 git 은 이력·원격 전용.** 지금과 똑같이 쓰고, 고친 뒤 커밋·푸시만 더한다. 클라우드 세션은 GitHub 사본에서 돌고, 거기서 고친 것은 pull 로 받는다. 변경이 가장 작다.
- **B안 — 시스템 폴더를 OneDrive 동기화에서 빼고 git 만으로 동기화.** 충돌 사본이 원천적으로 사라지지만, OneDrive 의 선택적 동기화로 하위 폴더만 제외하는 것이 깔끔하지 않아 구조를 바꿔야 한다.

**A안을 권한다.** 지금 구조를 건드리지 않고 이득만 가져온다.

## 두 PC 운영 — OneDrive 와 git 이 같은 파일을 나른다

작업본이 OneDrive 로도 동기화되므로, 다른 PC 에서 고친 내용은 **git 으로 받기 전에 이미 파일로 도착해 있다.** 그래서 보통의 `git pull` 이 "local changes would be overwritten" 으로 거부되는 일이 생긴다. 파일 내용은 같은데 git 이 그것을 모르기 때문이다.

**규칙은 하나다. OneDrive 동기화가 끝난 것을 먼저 보고, 그 다음에 이력을 맞춘다.**

```
공통\scripts\g.bat fetch
공통\scripts\g.bat status          # OneDrive 가 이미 가져온 것이 변경으로 보인다
공통\scripts\g.bat reset --hard origin/main
```

`reset --hard` 가 위험해 보이지만, **OneDrive 가 같은 내용을 이미 내려놓은 뒤라면 파일은 달라지지 않고 인덱스만 맞는다.** 반대로 이 PC 에서만 고친 것이 섞여 있으면 그것까지 지우므로, `g.bat status` 로 무엇이 바뀌었는지 먼저 본다.

- **한 번에 한 PC 에서만 작업한다.** OneDrive 는 충돌하면 병합하지 않고 `파일명-PC이름.md` 사본을 만든다(`README.md` 3항). git 을 붙였다고 이 규칙이 느슨해지지 않는다.
- **고친 PC 에서 바로 커밋·푸시한다.** 커밋하지 않고 다른 PC 로 넘어가면, 그 PC 에서는 파일만 바뀌고 이력이 없어 무엇이 달라졌는지 git 이 설명해 주지 못한다.
- 사건 자료는 git 에 없으므로 언제나 OneDrive 만 본다.

## 원격 — GitHub private

클라우드 세션은 원격 저장소에서 clone 하므로 GitHub(또는 동급 호스팅)이 필요하다.

- `gh` CLI 가 이 PC에 **설치되어 있지 않다**(`git` 2.55 는 있다). 저장소 생성은 웹에서 하거나 `winget install GitHub.cli` 로 깐다.
- 의뢰인 자료는 위 규칙으로 전부 빠지지만, **남는 471개도 업무 노하우**이다(문체가이드, 검색 전략, 계산 엔진, 지침 전문). private 이어야 하고, **법인 외부 호스팅에 올리는 것이 회사 규정상 가능한지 확인이 필요하다.** 이 문서가 답할 수 있는 사항이 아니다.

## 클라우드에서 되는 것과 안 되는 것

| 작업 | 클라우드 | 이유 |
|---|---|---|
| `6_손해배상계산` pytest 409건 | **된다** | 파이썬만 필요 |
| 계산 엔진·스크립트 수정 | **된다** | |
| 지침·명령어·스킬 정리 | **된다** | |
| docx 조립 로직 수정 | 된다(면수 확인은 못 함) | |
| LBOX 검색·하이라이트 | 안 된다 | 사용자가 로그인한 브라우저가 필요 |
| docx 면수 확인 | 안 된다 | Word COM(Windows). LibreOffice 는 한글 줄바꿈이 달라 적게 나온다 |
| 한글 양식 채우기 | 안 된다 | 한컴오피스 COM |
| 녹음 전사 | 안 된다 | 로컬 음성 모델 |
| 사건 작업 전반 | 안 된다 | 사건 자료가 clone 에 없다(의도한 바) |

**사건 작업을 집에서 이어가는 수단은 여전히 Remote Control 이다.** git 분리는 시스템 개발을 클라우드로 옮기는 것이지 사건 작업을 옮기는 것이 아니다.

## 고쳐야 할 문서

| 파일 | 무엇을 |
|---|---|
| `.gitignore` | 위 규칙 추가 — **완료** |
| `README.md` | "git은 쓰지 않는다" 단락을 `.git` 분리 방식으로 고치고, 5항 휴대폰 표를 갱신 |
| `CLAUDE.md`(루트) · `AGENTS.md` | "git은 쓰지 않는다" 를 "사건 자료는 git 에 올리지 않는다" 로 |
| `시작하기.md` | 두 번째 PC 절차에 저장소 설정 한 단계 추가 |
| `백업.ps1` | 남길지 정한다. git 이 시스템을 맡으면 zip 은 사건 산출물만 담으면 된다 |
| `공통/운영.md` | 커밋 시점 규칙(지침을 고쳤으면 커밋) |

## 진행 상황

| | 상태 |
|---|---|
| 1. `.gitignore` 보강, 혼입 134건 제외 | **완료** |
| 2. 외부 호스팅 가부 | **사용자 판단으로 진행** (본인 소유 시스템) |
| 3. `.git` 분리 설정, 래퍼 3개 | **완료** (`g.bat`, `g.ps1`, `repo-setup.ps1`) |
| 4. 첫 커밋 | **완료** — `023d3b7`, 471개 파일 47,750줄 |
| 5. **GitHub private 저장소 생성·연결** | **남음** ← 사용자가 저장소를 만들고 주소를 주면 연결한다 |
| 6. 문서 갱신(`README.md`·`CLAUDE.md`·`시작하기.md`) | 남음 |
| 7. 두 번째 PC 설정 | 남음 |
| 8. 시험: 클라우드에서 pytest 409건 | 남음 |

`gh` CLI 는 `winget install GitHub.cli` 가 끝내 완료되지 않아 설치하지 못했다. **없어도 된다.** GitHub 웹에서 빈 private 저장소를 하나 만들고 그 주소를 주면 `repo-setup.ps1 -원격` 으로 연결하고 푸시한다. 인증은 Git Credential Manager 가 브라우저로 처리한다.
